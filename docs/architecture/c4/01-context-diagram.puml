@startuml 01-context-diagram
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Context.puml

LAYOUT_WITH_LEGEND()

title System Context - WhatsApp Dashboard

' =====================================================================
' Pessoas (Atores do Sistema)
' =====================================================================
Person(user, "Usuário Final", "Acessa o dashboard web para visualizar conversas e métricas do WhatsApp")
Person(admin, "Administrador", "Gerencia configurações do sistema, usuários e integrações")

' =====================================================================
' Sistema Principal (Foco do Diagrama)
' =====================================================================
System(whatsapp_dashboard, "WhatsApp Dashboard", "Sistema web que monitora, processa e exibe métricas e conversas do WhatsApp em tempo real")

' =====================================================================
' Sistemas Externos
' =====================================================================
System_Ext(evolution_api, "Evolution API", "API de terceiros que fornece integração com WhatsApp Business, enviando webhooks e permitindo consultas de mensagens")
System_Ext(whatsapp, "WhatsApp Business", "Plataforma de mensagens corporativa da Meta")

' =====================================================================
' Relacionamentos (Interações entre os elementos)
' =====================================================================
Rel(user, whatsapp_dashboard, "Visualiza conversas, métricas e dashboards", "HTTPS")
Rel(admin, whatsapp_dashboard, "Configura integrações e gerencia usuários", "HTTPS")

Rel(whatsapp_dashboard, evolution_api, "Recebe webhooks de mensagens e consulta dados", "HTTPS/REST API")
Rel(evolution_api, whatsapp, "Conecta e sincroniza mensagens", "WhatsApp Protocol")

' =====================================================================
' Notas explicativas (opcional)
' =====================================================================
note right of whatsapp_dashboard
  **Sistema Principal**
  Processa mensagens recebidas
  via webhooks, armazena dados
  e exibe em dashboard interativo
end note

@enduml
